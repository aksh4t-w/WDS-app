<h3>Recently Added</h3>
<div>
  <% books.forEach(book => { %>
  <div class="container">
    <a href="/books/<%= book.id %>">
      <img height="250" width="180" src="<%= book.coverImagePath %>" />
      <div><%= book.title %></div>
    </a>
  </div>
  <% }) %>
</div>
